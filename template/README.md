## 代码生成
<plugin>
     <groupId>org.jooq</groupId>
     <artifactId>jooq-codegen-maven</artifactId>
     <configuration>
        <skip>true</skip> <!-- 默认情况下应该跳过生成 -->
     </configuration>
</plugin>

##